import { Router } from "express";
export const createSupportRouter = () => {
    const router = Router();
    // Example: router.get("/tickets", ...);
    return router;
};
//# sourceMappingURL=platform-support.routes.js.map