import { Router } from "express";
export const createPlatformAdminRouter = () => {
    const router = Router();
    // Example: router.get("/overview", ...);
    return router;
};
//# sourceMappingURL=platform-admin.routes.js.map